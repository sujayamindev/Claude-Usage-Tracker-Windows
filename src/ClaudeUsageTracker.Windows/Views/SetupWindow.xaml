<ui:FluentWindow x:Class="ClaudeUsageTracker.Windows.Views.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Claude Usage Tracker — Setup"
        Icon="/Assets/AppIcon.ico"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="Claude Usage Tracker — Setup"/>
        <StackPanel x:Name="ManualEntryPanel" Grid.Row="1" Margin="20">
            <TextBlock Text="Connect your Claude account" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
            <ui:Button x:Name="SignInWithBrowserButton" Content="Sign in with browser" Appearance="Primary"
                       HorizontalAlignment="Stretch" Margin="0,0,0,12" Click="SignInWithBrowserButton_Click"/>
            <TextBlock Text="OR" HorizontalAlignment="Center" Foreground="#8A8A8E" Margin="0,0,0,12"/>
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,12"
                       Text="Paste your Claude.ai session key. You can find it in your browser's DevTools under Application → Cookies → claude.ai → sessionKey."/>
            <TextBox x:Name="SessionKeyBox" Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" Margin="0,0,0,8"/>
            <TextBlock x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,8"/>
            <ui:Button x:Name="ConnectButton" Content="Connect" Appearance="Primary" HorizontalAlignment="Stretch" Click="ConnectButton_Click"/>
        </StackPanel>
        <Grid x:Name="BrowserSignInPanel" Grid.Row="2" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <wv2:WebView2 x:Name="SignInWebView" Grid.Row="0"/>
            <ui:Button x:Name="CancelSignInButton" Grid.Row="1" Content="Cancel" Appearance="Secondary"
                       HorizontalAlignment="Stretch" Margin="20,12,20,20" Click="CancelSignInButton_Click"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
