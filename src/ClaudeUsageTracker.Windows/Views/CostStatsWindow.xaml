<ui:FluentWindow x:Class="ClaudeUsageTracker.Windows.Views.CostStatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Claude Usage Tracker — Cost Stats"
        Icon="/Assets/AppIcon.ico"
        Width="420" Height="480"
        MinWidth="360" MinHeight="360"
        ShowInTaskbar="True"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="Claude Usage Tracker — Cost Stats"/>

        <Grid Grid.Row="1" Margin="20,12,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,4">
                <TextBlock Text="Claude Code Cost" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="From Claude Code CLI's own reported cost per session" FontSize="11" Foreground="#8A8A8E"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,12,0,16">
                <TextBlock x:Name="TotalText" Text="$0.00 total" FontSize="28" FontWeight="Bold"/>
                <TextBlock x:Name="EmptyStateText" Text="No cost data recorded yet" FontSize="12" Foreground="#8A8A8E" Visibility="Collapsed"/>
            </StackPanel>

            <ListView x:Name="DailyTotalsListView" Grid.Row="2">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Date" Width="180" DisplayMemberBinding="{Binding DateLabel}"/>
                        <GridViewColumn Header="Cost" Width="120" DisplayMemberBinding="{Binding CostLabel}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <ui:Button x:Name="RefreshButton" Grid.Row="3" Content="Refresh" HorizontalAlignment="Right" Margin="0,12,0,0" Click="RefreshButton_Click"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
